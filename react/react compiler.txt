React Compiler是一个编译器，仅在构建时使用，可以自动优化React应用程序。其中还包含一个ESlint插件用于在编辑器中显示编译器的分析结果

作用：
React Compiler会自动对代码进行记忆化处理以优化程序。其会利用JS和React规则，对组件和钩子中的值/值组进行记忆化。如果检测到规则的破坏，则会自动跳过对这些组件或钩子的优化（可以使用对应的ESlint插件来帮助你遵守React规则）。
目前可以跳过组件的级联重新渲染；缓存组件/钩子内的昂贵计算。