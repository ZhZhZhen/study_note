1、无连接的：即请求完则断开连接，这里的连接指的是传输层的TCP连接。（仅针对http1.0，后续默认为长连接，一次TCP连接可处理多个http请求，并且可在响应未达到时连续发送请求）。
2、媒体独立的：http协议可传输任意类型的数据，通过协议头的Content-Type指定MIME-type来标识数据类型。
3、无状态的：对事务处理没有记忆能力。缺失状态意味着如果后续处理需要前面的信息，则必须重传。