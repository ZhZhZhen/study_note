发生在基于流式传输（如TCP）的通信中，因为其不保证数据包的边界，而是将数据视为连续的字节流。这导致前一个数据可能与后一个数据相连（粘包），或者数据只接收了一半（半包）。

解决方法：通常会在内存中额外维护一个缓冲区，并使用工作线程进行数据的拼接工作，并通过以下方式区分完整数据
1、自定义数据协议，如在开头加入每一段数据的长度。
2、固定数据的大小，字符长度不够时采用占位符补齐。
3、使用特殊符号分割数据。