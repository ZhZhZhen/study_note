1、let与var的区别
#1、var不受块级作用域的影响；而块级作用域里的let，外部无法访问。
#2、var被变量提升，可以先使用后申明；let收到临时性死区的影响，必须先声明后使用
#3、var允许变量同名冲突；let则不允许在同一个块中出现同名变量
#4、在顶层所声明的var变量会称为global的属性，而let不会（ES3全局执行上下文通过变量对象记录var，而该变量对象就是global；ES6全局执行上下文通过词法环境的对象环境记录来记录var，该记录会和global有映射关系）

2、const
用于定义常量，只能在所声明的作用域里访问，声明的时候必须赋值，且不能修改