定义：
闭包就是能够读取其他函数内部变量的函数
1、从理论角度：所以的js函数都是闭包。因为它们被创建的时候，即通过内部[[scope]]属性存储了所在作用域的词法环境（es3中存储的是执行上下位的scope属性的拷贝），由于作用域链的存在所以可以访问父函数声明的变量。
2、从实践角度：会更加严格一点。满足#1即使创建它的上下位已经销毁，该函数仍存在；#2在代码中使用了自由变量
*自由变量是指在该函数中使用的，但既不是函数参数也不是该函数的局部变量的变量